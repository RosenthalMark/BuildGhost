# Asset Layout

Canonical asset root is:

- `GhostOps-Terminal/assets/core/` for shared shell assets (startup video, main logo, universal art)
- `GhostOps-Terminal/assets/modules/<module-name>/` for module-specific assets (boot clips, module art, etc.)

Current module naming convention in runtime paths is lowercase (example: `scrapetag`).
